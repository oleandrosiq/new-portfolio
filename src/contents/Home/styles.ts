import { styled } from '../../styles/styled';

export const ContainerCenter = styled('div', {
  width: '100%',
  minHeight: '100vh',
  maxWidth: '95%',
  margin: '0 auto',

  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});