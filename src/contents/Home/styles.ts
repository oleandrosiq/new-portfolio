import { styled } from '../../styles/styled';

export const Container = styled('div', {
  width: '100%',
  maxWidth: '95%',
  margin: '0 auto',

  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});